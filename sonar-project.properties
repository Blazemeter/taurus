# Required metadata
sonar.projectKey=taurus
sonar.projectName=taurus
sonar.projectVersion=1.0

# Comma-separated paths to directories with sources (required)
sonar.sources=bzt

# Language
sonar.language=py

# Encoding of the source files
sonar.sourceEncoding=UTF-8
