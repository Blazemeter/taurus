---
execution:
    - scenario:
        requests:
            - http://localhost/
      iterations: 100
    - scenario:
        requests:
            - http://localhost/
      iterations: 100
    - scenario:
        requests:
            - http://localhost/
      iterations: 100
