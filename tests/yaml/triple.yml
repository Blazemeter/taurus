---
execution:
    - scenario:
        requests:
            - http://localhost/100
      iterations: 100
    - scenario:
        requests:
            - http://localhost/200
      iterations: 100
    - scenario:
        requests:
            - http://localhost/300
      iterations: 100
