---
execution:
- concurrency: 1
  hold-for: 1m
  scenario: BZM-API
scenarios:
  BZM-API:
    default-address: https://a.blazemeter.com
    headers:
      api_key: ${myApiKey}
    requests:
    - url: ${basePath}/tests
settings:
  env:
    basePath: /api/v4
    myApiKey: TOKEN

