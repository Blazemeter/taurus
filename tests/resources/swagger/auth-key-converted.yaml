---
execution:
- concurrency: 1
  hold-for: 1m
  scenario: BZM-API
scenarios:
  BZM-API:
    default-address: https://a.blazemeter.com
    headers:
      api_key: ${myApiKey}
    variables:
      myApiKey: TOKEN
    requests:
    - url: /api/v4/tests

