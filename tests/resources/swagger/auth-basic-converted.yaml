---
execution:
- concurrency: 1
  hold-for: 1m
  scenario: BZM-API
scenarios:
  BZM-API:
    default-address: https://a.blazemeter.com
    headers:
      Authorization: 'Basic ${__base64Encode(${auth})}'
    variables:
      auth: USER:PASSWORD
    requests:
    - url: /api/v4/tests

