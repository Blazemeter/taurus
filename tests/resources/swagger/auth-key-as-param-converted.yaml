---
execution:
- concurrency: 1
  hold-for: 1m
  scenario: BZM-API
scenarios:
  BZM-API:
    default-address: https://a.blazemeter.com
    requests:
    - body:
        api_key: ${myApiKey}
      url: ${basePath}/tests
    variables:
      myApiKey: TOKEN
settings:
  env:
    basePath: /api/v4

