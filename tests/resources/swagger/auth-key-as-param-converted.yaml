---
execution:
- concurrency: 1
  hold-for: 1m
  scenario: BZM-API
scenarios:
  BZM-API:
    default-address: https://a.blazemeter.com
    variables:
      basePath: /api/v4
      myApiKey: TOKEN
    requests:
    - body:
        api_key: ${myApiKey}
      url: /${basePath}/tests

