---
execution:
- concurrency: 1
  hold-for: 1m
  scenario: /tests
scenarios:
  /tests:
    default-address: ${defaultAddress}
    headers:
      api_key: ${myApiKey}
    requests:
    - url: ${default-path}/tests
settings:
  env:
    default-path: /api/v4
    defaultAddress: https://a.blazemeter.com
    myApiKey: TOKEN

